using System;
using System.Collections.Generic;
using System.IO;
using System.IO.Compression;
using Backups.Algorithms;
using Backups.Entities;
using Backups.Interfaces;
using Backups.Models;

namespace Backups
{
    internal class Program
    {
        private static void Main()
        {
            /*const string repositoryPath = "C:\\Users\\Дмитрий\\OOP";
            const string path = "C:\\Users\\Дмитрий\\Downloads\\";
            const string name = "Lab3_07.xlsx";
            const int numberBackupTask = 1;
            const int idOfBackupTask = 1;
            var backupService = new BackupService();
            var repository = new Repository(repositoryPath);
            var backupTask = backupService.AddBackupTask(repository, new SingleStorage(), new List<RestorePoint>(), numberBackupTask, path, name, idOfBackupTask);
            backupService.Execute(backupTask);*/
        }
    }
}